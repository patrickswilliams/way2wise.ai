# Netlify configuration for way2wise.ai

[build]
  publish = "."

# Serve the thank-you page for the extensionless /thanks path,
# so form redirects resolve whether they target /thanks or /thanks.html.
[[redirects]]
  from = "/thanks"
  to = "/thanks.html"
  status = 200
